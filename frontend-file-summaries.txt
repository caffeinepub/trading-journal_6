{
  "frontend/src/pages/RiskManagementPage.tsx": "Risk management page with glassmorphism cards for live P&L status, account settings form, and backup/restore functionality.",
  "frontend/src/components/LoginScreen.tsx": "Full-screen branded login page with trading background image, feature highlights, and an \"Login with Internet Identity\" button using the useInternetIdentity hook.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal dialog for first-time users to enter their display name on initial login, using the useSaveCallerUserProfile mutation hook.",
  "frontend/src/hooks/useGetCallerUserProfile.ts": "React Query hook that fetches the authenticated user's profile from the backend, with custom isLoading/isFetched states that account for actor initialization to prevent modal flash.",
  "frontend/src/hooks/useSaveCallerUserProfile.ts": "React Query mutation hook that saves the authenticated user's profile to the backend and invalidates the currentUserProfile query on success.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations — adds `useGetStrategies` (fetches user's strategy list), `useSaveStrategy` (saves a new strategy), `useDeleteStrategy` (removes a strategy), and rewrites `useUpdateTrade` to use the correct `UpdateTradeRecord` interface with `{ id, update }` params, invalidating trades, riskStatus, and strategies caches on success.",
  "frontend/src/components/Layout.tsx": "App shell with glassmorphism sidebar, enlarged 22px lucide-react icons for each nav item, active state indicators, user profile display, and responsive mobile overlay.",
  "frontend/src/App.tsx": "Main app with TanStack Router, QueryClient, ThemeProvider (dark), and an `AuthGuard` component that shows `LoginScreen` for unauthenticated users, a loading spinner during initialization, and `ProfileSetupModal` (always rendered, with `open={showProfileSetup}`) for new users — all pages are protected behind authentication.",
  "frontend/src/pages/DashboardPage.tsx": "Dashboard with 7-day P&L area chart (Recharts) at top, glassmorphism stat cards, recent trades list with neon Buy/Sell badges, and monthly stats panel.",
  "frontend/src/index.css": "Global styles with OKLCH dark navy color system, glassmorphism utility classes (.glass-card, .glass-sidebar), neon Buy/Sell button/badge classes, conviction star styles, and profit/loss color utilities.",
  "frontend/tailwind.config.js": "Tailwind config with Space Grotesk/Inter fonts, OKLCH color tokens, custom border radius, neon box-shadow utilities, and animation keyframes.",
  "frontend/src/pages/NewTradePage.tsx": "New trade form with all fields including StrategyAutocomplete for smart strategy suggestions — fixes the TypeScript error by correcting the post-submit navigation from `/trades` to the valid route `/trade-log`.",
  "frontend/src/pages/TradeLogPage.tsx": "Trade log page with all existing filter/display functionality plus an \"Edit Trade\" button in each expanded trade card that opens the `EditTradeModal` with the selected trade pre-filled.",
  "frontend/src/pages/AnalyticsPage.tsx": "Analytics page with all existing charts plus a new \"Strategy Performance\" section featuring a sortable table (5 metrics per strategy with best-strategy gold highlighting) and a Recharts bar chart of Total P&L per strategy with the best performer highlighted in gold.",
  "frontend/src/pages/SettingsPage.tsx": "Settings page with About and Theme sections plus a new \"Strategy Manager\" section that lists all saved strategies with individual Delete buttons, an empty-state message, and loading indicator.",
  "frontend/src/lib/tradeUtils.ts": "Trade utility functions — fixed `calcPnl` to be direction-aware (Buy/Long uses exit−entry, Sell/Short uses entry−exit), fixed `calcRiskRewardRatio` to use `(target−entry)/(entry−stopLoss)`, fixed `calcRiskPerTrade` to use `(entry−stopLoss)*qty`, added `getPnLColorClass` for Emerald/Red color coding, added `formatMarketType` to map enum values to display labels, and fixed `exportToCsv` to use `t.marketType` instead of the removed `t.tradeType`.",
  "frontend/src/components/StrategyAutocomplete.tsx": "Reusable autocomplete input for strategy names that fetches saved strategies via `useGetStrategies`, filters suggestions using both substring and abbreviation matching (e.g. \"SR\" → \"Support & Resistance\"), and supports full keyboard navigation (arrow keys, Enter, Escape) with a glassmorphism dropdown.",
  "frontend/src/components/EditTradeModal.tsx": "Modal dialog for editing an existing trade with pre-filled fields for Entry, Stop Loss, Target, P&L, Strategy (using StrategyAutocomplete), Emotion, and Notes — calls `useUpdateTrade` with inline loading state, shows errors without closing, and toasts on success."
}